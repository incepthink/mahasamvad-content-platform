// The DLO workspace is mounted by the persistent app shell. Keeping this route
// page empty prevents a second workspace instance while still giving Next.js a
// concrete /dlo route.
export default function DloPage() {
  return null;
}
